name: Keep Render Awake

on:
  schedule:
    - cron: '*/14 * * * *'  # Cada 14 minutos
  workflow_dispatch:  # Permite ejecutar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s https://codigopy-api.onrender.com/health
